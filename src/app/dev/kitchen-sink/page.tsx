import Link from 'next/link';
import type { Metadata } from 'next';
import * as Bits from '@/components/reactbits';
import { demo } from '@/content/dev';
export const metadata: Metadata = { title: demo.title, robots:{index:false,follow:false} };
export default function KitchenSink(){const examples=[<Bits.SplitText key="text" text={demo.title}/>,<Bits.CountUp key="count" to={demo.count}/>,<Bits.ScrollReveal key="reveal"><p>{demo.note}</p></Bits.ScrollReveal>,<Bits.AnimatedContent key="animated"><p>{demo.note}</p></Bits.AnimatedContent>,<Bits.LogoLoop key="logos" items={demo.components}/>,<Bits.Magnet key="magnet"><Link href="/contact">{demo.items[2]?.label}</Link></Bits.Magnet>,<Bits.GradualBlur key="blur"><p className="min-w-[800px]">{demo.note}</p></Bits.GradualBlur>,<Bits.CardNav key="nav" items={demo.items}/>,<Bits.StaggeredMenu key="menu"><Bits.FlowingMenu items={demo.items}/></Bits.StaggeredMenu>,<Bits.ScrollStack key="stack">{demo.steps.map(s=><article className="licence-card" key={s.name}>{s.name}<p>{s.detail}</p></article>)}</Bits.ScrollStack>,<Bits.Stepper key="stepper" steps={demo.steps}/>,<Bits.Masonry key="masonry">{demo.steps.map(s=><article className="licence-card" key={s.name}>{s.name}<p>{s.detail}</p></article>)}</Bits.Masonry>,<Bits.FlowingMenu key="flow" items={demo.items}/>,<Bits.TiltedCard key="tilt"><p>{demo.note}</p></Bits.TiltedCard>,<Bits.Carousel key="carousel" label={demo.title}>{demo.steps.map(s=><p key={s.name}>{s.detail}</p>)}</Bits.Carousel>,<Bits.Lanyard key="lanyard"><h3>{demo.card}</h3><p>{demo.status}</p></Bits.Lanyard>,<div className="relative h-32" key="dots"><Bits.DotGrid/></div>];return <main className="container py-16"><h1 className="display text-5xl">{demo.title}</h1><p>{demo.note}</p>{examples.map((example,i)=><section className="py-12 border-b border-rule" key={demo.components[i]}><h2 className="meta mb-6">{demo.components[i]}</h2>{example}</section>)}</main>;}
